name: Node Test
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master, develop ]

jobs:
  build:
    name: Node Build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
      - name: Add Target
        run: rustup target add wasm32-unknown-unknown
      - name: Build
        run: cd examples && cargo build --all-features
